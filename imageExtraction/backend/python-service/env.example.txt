# ===============================================
# Voter ID Extraction System - Configuration
# ===============================================
# Copy this file to .env and fill in your values

# --- Server Configuration ---
DEBUG=False
# Set to True only for development. NEVER use True in production!

# File retention period (hours)
FILE_RETENTION_HOURS=24

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# --- Authentication (Optional) ---
AUTH_ENABLED=False
# Set to True to enable API key authentication
# API_KEYS=your_api_key_1,your_api_key_2
# Generate keys using: python auth.py

# --- Azure OpenAI Configuration (Optional but Recommended) ---
# Used for intelligent voter ID text cleaning and formatting
AZURE_OPENAI_API_KEY=your_azure_openai_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# --- Azure Computer Vision Configuration (Optional) ---
# Used for OCR when local Tesseract confidence is low (<70%)
AZURE_VISION_KEY=your_azure_vision_key_here
AZURE_VISION_ENDPOINT=https://your-vision-resource.cognitiveservices.azure.com/

# --- OCR Configuration ---
# Tesseract path (auto-detected on Windows, set manually for Linux/Mac)
# TESSERACT_CMD=/usr/bin/tesseract

# OCR DPI setting (300-600, higher = better quality but slower)
OCR_DPI=400

# Confidence threshold for using Azure fallback (0.0-1.0)
# Lower = more Azure API calls, Higher = more local processing
OCR_CONFIDENCE_THRESHOLD=0.7

# --- Performance Settings ---
# Maximum concurrent background tasks
MAX_BACKGROUND_WORKERS=3

# Maximum PDF file size (bytes)
# Default: 50MB = 52428800
# MAX_FILE_SIZE=52428800

# --- Logging ---
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# --- Notes ---
# 1. Azure services are OPTIONAL - system works without them
# 2. Without Azure: Uses local Tesseract OCR (free, slightly less accurate)
# 3. With Azure: Better accuracy, but costs ~$0.001-0.01 per page
# 4. Recommended: Start without Azure, enable if accuracy is insufficient
# 5. For Hindi text support, install Tesseract Hindi language data

# --- Quick Start ---
# 1. Copy this file: cp env.example.txt .env
# 2. Edit .env with your values
# 3. Restart server: python app.py
# 4. Check logs: tail -f app.log

